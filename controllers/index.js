const {signup, signin, current, logout, updateAvatar,updateUserInfo, emailVerify,verify} = require ('./auth')

module.exports = {
    signup,
    signin,
    current,
    logout,
    updateAvatar,
    updateUserInfo,
    emailVerify,
    verify
};
